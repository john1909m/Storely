spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://DB_HOST:5432/storely
    username: prod_user
    password: prod_pass

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration